#include <iostream.h>
#include <conio.h>

void main()
{
 int m = 166;
 cout<<"(m>=0 && m<=150) => "<<(m>=0 && m <= 150)<<" = SALAH"<<endl;
 cout<<"(m>=0 || m<=150) => "<<(m>=0 || m <= 150)<<" = BENAR"<<endl;
 getch();
}